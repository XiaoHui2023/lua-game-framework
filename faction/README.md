# faction

阵营模块，用对象封装阵营身份和阵营关系。

- 阵营对象可被单位、玩家或规则系统引用。
- 关系判断集中在对象层。
- 保持纯框架数据，不绑定地图阵营实现。

## 设计特性

### 阵营对象

阵营以对象表达，而不是散落的字符串或数字。上层可以把阵营对象挂到玩家、单位或场景规则上。

### 关系收口

敌友、中立等关系适合在对象层统一处理。战斗、目标选择和 UI 显示读取同一份阵营语义。

## 目录

```text
init.lua   # 阵营模块门面
object.lua # 阵营对象
```
