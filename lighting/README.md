# lighting

光照模块，用对象表达可被创建、修改和释放的光照实例。

- 光照对象保存框架侧状态。
- 引擎创建和销毁由运行时或上层接入。
- 适合场景、技能和特效系统复用。

## 设计特性

### 对象封装

光照不是一次性函数调用，而是可持有的对象。持续光、范围光和临时光都可以共享生命周期处理。

### 引擎边界

框架层只描述光照意图。具体渲染、句柄和资源 ID 不写死在通用模块里。

## 目录

```text
init.lua   # 光照模块门面
object.lua # 光照对象
```
