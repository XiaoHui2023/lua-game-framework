# log

日志类型模块，提供框架内统一的日志等级和日志 payload 类型。

- 类型定义集中在 `types.lua`。
- 具体输出后端由 runtime 注入。
- 框架模块只依赖抽象日志语义。

## 设计特性

### 类型集中

日志等级、日志内容结构和可选字段集中声明，避免每个模块对日志 payload 各写一套解释。

### 后端分离

框架不关心日志最终进控制台、文件还是引擎面板。运行时接入实际输出能力。

## 目录

```text
init.lua  # 日志模块门面
types.lua # 日志类型声明
```
